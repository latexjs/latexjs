ssh root@london.latexjs.org "docker-compose pull && docker-compose down && docker-compose up -d"
ssh root@sanfran.latexjs.org "docker-compose pull && docker-compose down && docker-compose up -d"
ssh root@singapore.latexjs.org "docker-compose pull && docker-compose down && docker-compose up -d"
